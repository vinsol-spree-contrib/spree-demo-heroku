
<div class="modal fade" id="modal_search">
  <div class="modal-dialog search-modal-full-width" role="document">
    <div class="modal-content">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
      <div class="modal-body">


        <% @taxons = @taxon && @taxon.parent ? @taxon.parent.children : Spree::Taxon.roots %>
        <%= form_tag spree.products_path, method: :get do %>
<!--           <div class="form-group">
            <%# cache(cache_key_for_taxons) do %>
              <%#= select_tag :taxon,
                    options_for_select([[Spree.t(:all_departments), '']] +
                                          @taxons.map {|t| [t.name, t.id]},
                                          @taxon ? @taxon.id : params[:taxon]), 'aria-label' => 'Taxon', class: "form-control" %>
            <%# end %>
          </div> -->
          <div class="search-container center-block">
            <%= search_field_tag :keywords, params[:keywords], placeholder: Spree.t(:search), class: "form-control input-lg" %>
            <%= button_tag '', name: nil, class: "fa fa-search" %>
          </div>

        <% end %>

      </div>

    </div>
  </div>
</div>
