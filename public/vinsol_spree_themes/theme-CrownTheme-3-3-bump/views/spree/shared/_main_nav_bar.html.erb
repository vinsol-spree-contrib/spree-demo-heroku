<nav class="col-md-6 col-sm-6 navbar navbar-default text-center hidden-xs">
  <div id="main-nav-bar" class="collapse navbar-collapse js-navbar-collapse">
    <ul class="nav navbar-nav" data-hook>
      <% taxonomy = Spree::Taxonomy.first %>
        <li class="dropdown mega-dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= taxonomy.name %> <i class="fa fa-angle-down pull-right"></i></a>
          <ul class="dropdown-menu">
            
            <% taxonomy.root.children.each_with_index do |taxon, i| %>
              <li>
                <%= link_to taxon.name, 't/' + taxon.permalink  %>
              </li>
            <% end %>
          </ul>
        </li>
    </ul>
  </div>
</nav>
