<%= render :partial => 'spree/shared/error_messages', :locals => { :target => @spree_user } %>
<div class="col-md-6 col-md-offset-3" id="forgot-password">
  <div class="panel panel-custom panel-top-margin">
    <div class="panel-heading">
      <h3 class="panel-title"><%= Spree.t(:forgot_password) %></h3>
    </div>
    <div class="panel-body">
      <div class="form-group"><%= Spree.t(:instructions_to_reset_password) %></div>

      <%= form_for Spree::User.new, :as => :spree_user, :url => spree.reset_password_path do |f| %>
        <div class="form-group">
          <%= f.label :email, Spree.t(:email) %>
          <%= f.email_field :email, :class => "form-control" %>
        </div>
        <div class="form-group">
          <%= f.submit Spree.t(:reset_password), :class => 'btn btn-site-green btn-block btn-lg' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
